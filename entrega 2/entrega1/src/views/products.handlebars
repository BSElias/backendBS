<header>
    <h1 class="titulo">Products:</h1>
</header>

<main>
    <section class="products__table">

        <div class="table__categories">
            {{#each products}}
            {{#if this.available}}
            <article class="categories__item">
                <div class="item__foto">
                    <div><a href={{this.thumbnail.[0]}} target="_blank"><img class="thumbnail"
                                src={{this.thumbnail.[0]}} alt={{this.thumbnail.[0]}}></a></div>
                </div>
                <div class="item__texto">
                    <div class="id">Id: {{this.id}}</div>
                    <div class="code">Code: {{this.code}}</div>
                    <div class="category">Category: {{this.category}}</div>
                    <div class="title">Title: {{this.title}}</div>
                    <div class="price">Price: {{this.price}}</div>
                    <div class="stock">Stock: {{this.stock}}</div>
                    <div class="description">Description: {{this.description}}</div>
                </div>
            </article>
            {{/if}}
            {{/each}}
        </div>

    </section>

</main>

{{!-- <header>
    <h1>Productos</h1>
</header>
<div class="container" style="display: flex; align-items: center; width: 90%">
    {{#if products.hasPrevPage}}
    <a href="/products?page={{products.prevPage}}{{#if sort}}&sort={{sort}}{{/if}}{{#if limit}}&limit={{limit}}{{/if}}"
        class="btn-normal" style="width: 60%; text-align: center; text-decoration:none; margin: 10px">Anterior</a>
    {{/if}}
    {{#if products.hasNextPage}}
    <a href="/products?page={{products.nextPage}}{{#if sort}}&sort={{sort}}{{/if}}{{#if limit}}&limit={{limit}}{{/if}}"
        class="btn-normal" style="width: 60%; text-align: center; text-decoration:none">Siguiente</a>
    {{/if}}

    <a href="/products?page={{products.page}}{{#if limit}}&limit={{limit}}{{/if}}&sort=asc" class="btn-normal"
        style="width: 10%; text-align: center; text-decoration:none; margin-left: 10px">⬇</a>
    <a href="/products?page={{products.page}}{{#if limit}}&limit={{limit}}{{/if}}&sort=desc" class="btn-normal"
        style="width: 10%; text-align: center; text-decoration:none; margin-left: 10px">⬆</a>
    <a href="/products?page={{products.page}}{{#if limit}}&limit={{limit}}{{/if}}" class="btn-normal"
        style="width: 10%; text-align: center; text-decoration:none; margin-left: 10px">X</a>

    <div style="margin: 10px; text-align: center; font-size: 12px;">
        <p>Página {{products.page}} de {{products.totalPages}}</p>
        <p>{{products.totalDocs}} Documentos</p>
    </div>
    <a id="go-cart-btn" class="btn-normal" style="width: 80%; text-align: center; text-decoration:none">Ir al
        carrito</a>
</div>

<div class="product-container">
    {{#each products.docs}}
    <div class="product-card">
        <img src={{this.thumbnail}} alt="Producto">
        <div class="info">
            <h2>{{this.title}}</h2>
            <p>{{this.description}}</p>
            <p>{{this.category}}</p>
            <p style="font-size: 12px;">Disponible: {{#if this.status}}✅{{else}}⛔{{/if}}</p>
            <div class="price">$ {{this.price}}</div>
            <button class="add-btn" data-id={{this._id}}>Añadir al carrito</button>
        </div>
    </div>
    {{/each}}
</div>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/js/products.js"></script> --}}